# Domain Context

This is a single-context repo for the Zoo game slice.

Agents should read:

- `CONTEXT.md` for the project language and domain terms.
- `DESIGN.md` for the current product and implementation shape.
- `docs/adr/` for architectural decisions.

Current ADR directory:

- `docs/adr/`
