name: Apply species welfare slice

on:
  push:
    branches:
      - feature/species-welfare-requirements

permissions:
  contents: write

jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/species-welfare-requirements
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt
      - name: Apply species welfare implementation
        run: python3 tools/apply_species_welfare.py
      - name: Format Rust
        run: cargo fmt --all
      - name: Commit slice
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add crates/zoo-core/src/lib.rs apps/web/src/App.tsx apps/web/src/styles.css
          git diff --cached --quiet && exit 0
          git commit -m "Implement species-specific habitat welfare"
          git push origin HEAD:feature/species-welfare-requirements
